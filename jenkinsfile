pipeline {
    agent {
		node {
			label "slave1"
		}
		parameters {
		  choice(name: 'CHOICE', choices: ['QA'], description: 'Pick something')
		  }
	}

    stages {
        stage('QA') {
            steps {
                echo 'this is for QA enviornment'
            }
        }
    }
}
