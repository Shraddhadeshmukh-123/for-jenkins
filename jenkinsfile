pipeline {
    agent {
		node {
			label "slave1"
		}
		parameters {
		  choice(name: 'CHOICE', choices: ['test'], description: 'Pick something')
		  }
	}

    stages {
        stage('test') {
            steps {
                echo 'this is for test enviornment'
            }
        }
    }
}
